# しりとり
# utf-8
S,T = input().split()
if S[-1] == T[0]:
    print("YES")
else:
    print("NO")
'''
この問題では、空白区切りの文字列が 2 つ与えられます。
文字列 S の末尾の文字と文字列 T の先頭文字を取得し、比較することで解くことができます。
input().split()を用いるとsplit()の引数に与えた文字で区切って値を受け取ることができます。
受け取った値は変数名[i]で参照することができます(例えば、変数名[1]で2番目の値を示します。i は 0 から数えることに注意してください)。
文字列[インデックス]で指定したインデックスの文字を取得することができます(例: 'mojiretsu'[0] は m を返す)。
インデックスは 0 から数えますが、負の数を指定することで末尾から数えて指定することも可能です。(例: 'mojiretsu'[-1]は u を返す)
'''

# 投票サイト
# utf-8
A,B = map(int,input().split())

if A > B:
    print(1)
elif A == B:
    print(0)
else:
    print(-1)

# 宿題管理
# utf-8
N, M = map(int, input().split(" "))
ANS = 0
for _ in range(N):
    A = int(input())
    if A <= M:
        ANS += 1

print(ANS)

# M件目の仕事
# utf-8
N, M = map(int, input().split(" "))
S = [None] * N
for i in range(N):
    S[i] = input()
print(S[M % N - 1])
'''
N 人で順番に仕事を担当するので、M を N で割ったあまりを用いることで、M 番目の仕事の担当者を求めることができます。
このとき、M % N は 0 ですが、仕事を担当するのは 3 番目のメンバー (配列では 2 番目の要素) になります。
Python の場合: 配列の最後尾の要素を -1 で指定することができるため、M % N - 1 とすることで正しいインデックスを指定できます。
'''
