2023年7月問題
<炎上案件>
# utf-8
due = int(input())
if due <= 3:
    print("BOMB")
    exit()
print("ONGOING")
'''
due = int(input())
この行では、標準入力から1行分の文字列を受け取り、int() 関数を使って整数に変換しています。これにより、納品までの日数を due 変数に格納します。
if due <= 3:
if 文は条件文を評価し、条件が真（True）の場合にブロック内のコードを実行します。ここでは due <= 3 という条件をチェックしています。つまり、due の値が3以下の場合に真となります。
print("BOMB")
条件が真の場合、つまり納品までの日数が3以下の場合に、"BOMB" という文字列を標準出力に表示します。
exit()
exit() 関数はプログラムを終了するための関数です。ここでは、条件が真の場合に "BOMB" を表示してプログラムを終了させています。これにより、それ以降のコードが実行されるのを防ぎます。
print("ONGOING")
もし条件が偽（False）の場合、つまり納品までの日数が3より大きい場合には、"ONGOING" という文字列を標準出力に表示します。
このコードでは、入力された日数が3以下の場合には「BOMB」と出力し、それ以外の場合には「ONGOING」と出力します。これにより、納品日前の進行中案件には「ONGOING」と出力し、納品まで残り3日以下の案件には「BOMB」と出力されるようになります。
'''

<不気味な数字>
# utf-8
n = int(input())
ans = n * 3
print(ans)
'''
n = int(input()):
input()関数を使って標準入力から1行の文字列を取得します。
int()関数を使って、取得した文字列を整数に変換します。これにより、nは整数として扱われるようになります。
ans = n * 3:
nは入力された整数です。この行では、nを3倍して計算した結果をansに代入しています。
n * 3は、入力されたnの値を3回足すことと同じです。
print(ans):
ansに格納された計算結果（nを3回足した結果）を出力します。
出力結果は改行を含んでいるため、末尾に改行が追加されます。
'''
